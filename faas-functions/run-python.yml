version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  run-python:
    lang: python3-flask
    handler: ./run-python
    image: run-python:latest
    environment:
      exec_timeout: 60s

      # waitress threads count
      thread_count: 20

